import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'SimulacionInversiones.app',
  appName: 'mobilTSS',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
